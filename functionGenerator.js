function * handleGenerator (){
   let name = yield "didier";

   console.log(`this is the function that handle the variable created 
    and also we are going  some data using dot next method
    `);
}